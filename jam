import time

waktu = int(input(" Berapa lama tidurmu :"))

for x in range (waktu, 0,-1) :
    seconds = x %60
    minute = int(x/60)%60
    hours = int (x / 3600) %60
    
    
    print (f"{hours:02}:{minute:02}:{seconds:02}")
    
    time.sleep(1)
print("BANGUNN!!")

